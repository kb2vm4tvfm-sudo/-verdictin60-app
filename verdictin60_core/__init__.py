"""Low-risk helpers extracted from app.py (Phase 1 refactor).

This package holds pure, side-effect-light helpers that were moved out of
the ``app.py`` monolith with no behavior change: settings persistence,
filename/display-name conversion, post-date/time formatting, and caption
validation. See each module's docstring for the list of moved functions.
"""
